module heap;

include std::mem;
include std::io = io;

@export
fn int main() {
    mut int mut*cs = std::mem::allocate<int, std::mem::Heap>(12u, &std::mem::default_heap);
    *cs = 3;
    return *cs;
}