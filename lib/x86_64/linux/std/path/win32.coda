module std::path::win32;

@export
string sep = "\\";
@export
string delimiter = ";";

fn string join(string[] parts);

fn string filename(string);

fn string stem(string);

fn string extension(string);

fn string parent(string);

fn bool is_absolute(string);

fn bool is_relative(string);

fn string normalise(string);

fn string absolute(string);