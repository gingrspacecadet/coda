module std;

include std::error = _;

@export
@inline
$fn #code::stmt assert(#code::expr expr) {
    return #{
        {
            if (!#(expr)) {
                fatal_error("Assertion failed: "#(expr.raw));
            }
        }
    };
}