module std::time;

struct Duration {
    uint64 seconds;
    uint32 nanoseconds;
}

struct Instant;

fn Instant now();

fn Duration elapsed(
    Instant start,
    Instant end
);

fn none sleep(Duration duration);